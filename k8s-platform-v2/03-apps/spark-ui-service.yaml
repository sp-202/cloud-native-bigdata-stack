apiVersion: v1
kind: Service
metadata:
  name: spark-ui
  namespace: default
spec:
  ports:
  - name: spark-ui
    port: 4040
    protocol: TCP
    targetPort: 4040
  selector:
    app: spark-connect-server
    spark-role: driver
  type: NodePort
