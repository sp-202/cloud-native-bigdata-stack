# =============================================================================
# k8s-git-sync Image Configuration
# =============================================================================
# k8s-git-sync version tag (e.g., v2-prod)
GIT_SYNC_IMAGE_VERSION=v2-prod

# Resulting image name (computed from above variable)
# GIT_SYNC_IMAGE=subhodeep2022/k8s-git-sync:${GIT_SYNC_IMAGE_VERSION}
